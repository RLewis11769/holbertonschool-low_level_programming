#include "holberton.h"

/**
 * wildcmp - compares 2 strings to determine if identical
 * @s1: pointer to first string to be compared
 * @s2: pointer to second string to be compared
 * Return: 1 if identical, 0 if not
 */

int wildcmp(char *s1, char *s2)
{

}
