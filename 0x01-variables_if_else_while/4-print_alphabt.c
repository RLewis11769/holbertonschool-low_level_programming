#include <stdio.h>

/**
 * main - 
 * Return: 
 */

int main(void)
{
int letter;
for(letter = 'a'; letter <= 'z'; letter++)
if (letter != 'e')
if (letter != 'q')
putchar(letter);
 putchar('\n');
return 0;
}
